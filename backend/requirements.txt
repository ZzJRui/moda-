fastapi
uvicorn[standard]
sqlalchemy
pydantic
python-multipart
python-dotenv
pillow

# 测试依赖（Phase 5）
pytest
httpx

# 图片处理（Phase 2.2）：rembg 抠图，Pillow 白底合成。
# ⚠️ 生产部署默认不装 rembg（onnxruntime ~18MB + 首次下载 u2net 模型 176MB，国内构建极慢）。
# 想启用抠图：设环境变量 REMBG_ENABLED=true 重新构建，并配合 IMAGE_PROCESSING_ENABLED=true。
# rembg[cpu]
